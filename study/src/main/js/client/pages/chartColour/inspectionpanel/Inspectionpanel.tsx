import React from 'react';

import Content from '../../../features/chartColour/inspectionpanel/components/Content';
import { initialize } from '../../../init';

const Inspectionpanel = () => {
  return <Content />;
};

initialize(Inspectionpanel);
