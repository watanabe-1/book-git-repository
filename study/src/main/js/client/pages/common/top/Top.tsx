import React from 'react';
import { initialize } from '../../../init';
import BodysHead from '../../../components/BodysHead';

/**
 *
 * @returns トップ
 */
const Top = () => {
  return <BodysHead title="ダッシュボード" />;
};

initialize(Top);
