import React from 'react';

import Content from '../../../features/books/export/components/Content';
import { initialize } from '../../../init';

const Export = () => {
  return <Content />;
};

initialize(Export);
